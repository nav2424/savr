# Environment Variables Template

# OpenAI API (for LLM features)
OPENAI_API_KEY=your_openai_api_key_here
PRIMARY_MODEL=gpt-4o
FALLBACK_MODEL=gpt-4o-mini

# OpenRouter (optional fallback)
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL=openrouter/auto

# Google Cloud Vision API (for OCR)
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Enable "Cloud Vision API" in your Google Cloud project
GOOGLE_CLOUD_VISION_API_KEY=your_google_cloud_vision_api_key_here

# Server Configuration
PORT=3000
MAX_RETRIES=2
INITIAL_DELAY_MS=600

# Expo/React Native (client-side)
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# RevenueCat (when EXPO_PUBLIC_ENABLE_PAYWALL=true)
# Get keys from: https://app.revenuecat.com → Project → API Keys
EXPO_PUBLIC_RC_IOS_API_KEY=appl_your_ios_key
# EXPO_PUBLIC_RC_ANDROID_API_KEY=goog_your_android_key
# EXPO_PUBLIC_REVENUECAT_WEB_API_KEY=rcb_your_web_key  # for Expo Go
# Optional: specific offering ID (e.g. "default"). If unset, uses the Current offering.
# EXPO_PUBLIC_REVENUECAT_OFFERING_ID=default
# Optional: entitlement identifier from RevenueCat (defaults to "pro"). Set this if your entitlement is named "Pro".
# EXPO_PUBLIC_REVENUECAT_ENTITLEMENT_ID=Pro

# Receipt OCR server (optional). For dev build on a physical device, set to your machine's LAN IP
# so the app can reach the OCR server (e.g. EXPO_PUBLIC_API_BASE=http://192.168.1.100:3000).
# If unset, Expo Go uses the Metro host; dev build may fall back to LLM and feel slower.
# EXPO_PUBLIC_API_BASE=http://localhost:3000

# Email verification redirect (production)
# Set to your deployed web app URL + /email-verified (e.g. https://yourapp.com/email-verified).
# Add this exact URL to Supabase Dashboard → Auth → URL Configuration → Redirect URLs.
# Leave unset to use app deep link (savr:///email-verification).
# EXPO_PUBLIC_EMAIL_VERIFICATION_REDIRECT_URL=https://yourapp.com/email-verified
